# 后端 API 地址配置
# 场景一：前后端在同一域名（Nginx 反向代理 / Vercel + 同一域名的后端）
#   保持 /api 即可，Vite 构建后会发相对路径请求
# 场景二：后端部署在独立域名（Railway / Render / 自建服务器）
#   改为完整地址，例如：
#   VITE_API_BASE=https://your-backend.onrender.com/api
VITE_API_BASE=/api
